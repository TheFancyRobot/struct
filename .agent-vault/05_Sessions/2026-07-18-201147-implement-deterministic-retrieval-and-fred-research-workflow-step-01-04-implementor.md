---
note_type: session
template_version: 2
contract_version: 1
title: step-01-04-implementor session for Implement Deterministic Retrieval and Fred Research Workflow
session_id: SESSION-2026-07-18-201147
date: '2026-07-18'
status: completed
owner: step-01-04-implementor
branch: agent/step-01-04-deterministic-retrieval-fred-workflow
phase: '[[02_Phases/Phase_01_walking_skeleton/Phase|Phase 01 walking skeleton]]'
context:
  context_id: SESSION-2026-07-18-201147
  status: completed
  updated_at: '2026-07-18T20:44:13.000Z'
  current_focus:
    summary: Advance [[02_Phases/Phase_01_walking_skeleton/Steps/Step_04_implement-deterministic-retrieval-and-fred-research-workflow|STEP-01-04 Implement Deterministic Retrieval and Fred Research Workflow]].
    target: '[[02_Phases/Phase_01_walking_skeleton/Steps/Step_04_implement-deterministic-retrieval-and-fred-research-workflow|STEP-01-04 Implement Deterministic Retrieval and Fred Research Workflow]]'
  resume_target:
    type: step
    target: '[[02_Phases/Phase_01_walking_skeleton/Steps/Step_04_implement-deterministic-retrieval-and-fred-research-workflow|STEP-01-04 Implement Deterministic Retrieval and Fred Research Workflow]]'
    section: Context Handoff
  last_action:
    type: saved
related_bugs: []
related_decisions: []
created: '2026-07-18'
updated: '2026-07-19'
tags:
  - agent-vault
  - session
context_status: completed
summary: STEP-01-04 completed with zero known confirmed defects; the final canonical maintained-host native Bun/PostgreSQL gate passed 262/262 tests with 1,506 assertions across 40 files after the complete review-remediation cycle.
context_summary: STEP-01-04 is complete after independent review and remediation. Authoritative current evidence is the scoped maintained-host native Bun/PostgreSQL 262/262 gate across 40 files; earlier counts remain historical checkpoints.
---

# step-01-04-implementor session for Implement Deterministic Retrieval and Fred Research Workflow

Use one note per meaningful work session. Record chronology, validation, and handoff state, but promote durable conclusions into phase, architecture, bug, or decision notes. See [[07_Templates/Note_Contracts|Note Contracts]].

## Objective

- Advance [[02_Phases/Phase_01_walking_skeleton/Steps/Step_04_implement-deterministic-retrieval-and-fred-research-workflow|STEP-01-04 Implement Deterministic Retrieval and Fred Research Workflow]].
- Leave a clean handoff if the work stops mid-step.

## Planned Scope

- Review [[02_Phases/Phase_01_walking_skeleton/Steps/Step_04_implement-deterministic-retrieval-and-fred-research-workflow|STEP-01-04 Implement Deterministic Retrieval and Fred Research Workflow]] before editing.
- Record changed paths and validation as the session progresses.

## Execution Log

<!-- AGENT-START:session-execution-log -->
- 20:11 - Created session note.
- 20:11 - Linked related step [[02_Phases/Phase_01_walking_skeleton/Steps/Step_04_implement-deterministic-retrieval-and-fred-research-workflow|STEP-01-04 Implement Deterministic Retrieval and Fred Research Workflow]].
- 20:44 - Completed implementation, repository/database validation, durable notes, context mirrors, and Vault integrity checks.
<!-- AGENT-END:session-execution-log -->
- 2026-07-18 retry 2 — Root verification found Node-driven Vitest could not collect `packages/fred-workflows/test/walking-skeleton.test.ts` because Fred 2.0.0's root bundle eagerly imports optional `bun:sqlite` adapters.
- Replaced product runtime value imports with type-only imports, deferred `createFred()` loading to the default Bun client factory, and returned native Fred workflow IR without eagerly loading Fred runtime code. The injected-client test boundary is now Node-compatible while the production worker remains Bun-owned.
- A concurrent Node/Bun gate run exposed hard-coded API/worker entrypoint smoke ports. Replaced them with OS-assigned ephemeral ports and proved both complete suites can run concurrently without collision.
- No git command was run; branch, commit, PR, review, and merge remain owned by the root orchestrator.
- 20:46 - Root orchestrator rejected attempt 1 after independently reproducing a Node Vitest `bun:` ESM loader failure; started fresh attempt 2 under the retry policy.
- 20:53 - Fresh retry fixed lazy Fred runtime loading and concurrent test port allocation. Root orchestrator independently reran root, real-database, migration, provider-load, and Vault gates successfully.
- 2026-07-18 retry 3 hygiene audit — Removed generated `tsconfig.tsbuildinfo` files from `packages/retrieval`, `packages/research-engine`, and `packages/fred-workflows`, and added the repository-level `*.tsbuildinfo` ignore rule. A bounded generated/cache-artifact scan found no other STEP-01-04 hygiene defect; the unrelated DuckDB spike build-info file already had spike-local ignore coverage and is now also covered at the root.
- No git command was run; the root orchestrator retains exclusive branch, staging, commit, push, PR, review, and merge control.
- 2026-07-18: Root orchestrator pushed the completed STEP-01-04 branch and opened ready-for-review PR [#1](https://github.com/TheFancyRobot/struct/pull/1) into `main`. Awaiting configured checks and code-review bots before merge.
- 2026-07-18 PR #1 review remediation completed without git or GitHub mutations. Root orchestrator retains publication, review-thread replies/resolution, automated re-review, and merge.
- 2026-07-18 PR #1 second review-remediation pass addressed the five unresolved threads: bounded worker-test shutdown escalation (`PRRT_kwDOTcucmc6SBiu9`); root-enforced Fred elapsed budget across optional factory execution paths (`PRRT_kwDOTcucmc6SBiu_`); document-level PostgreSQL FTS preservation and relevant-line fallback for terms/phrases spanning newlines (`PRRT_kwDOTcucmc6SBivA`, `PRRT_kwDOTcucmc6SBi8B`); and README alignment with startup Fred provider preflight (`PRRT_kwDOTcucmc6SBi8D`).
- The worker smoke helper now escalates SIGTERM to SIGKILL after a bounded two-second grace period. The Fred runtime wraps every workflow factory path in the configured `maxElapsedMs` budget and still releases the client. Retrieval retains document-level matches with a left-lateral locator lookup, preferring an exact line and otherwise anchoring the first line containing any normalized query lexeme.
- No git or GitHub command was run; the root orchestrator retains exclusive publication, thread resolution, re-review, and merge control.
- 2026-07-18 PR #1 third review-remediation pass addressed unresolved thread `PRRT_kwDOTcucmc6SBmEV`: ingestion and research polling now apply the configured spaced schedule independently before the loops are combined concurrently. A long-running poll in either queue no longer delays the other queue, while a loop failure still propagates and interrupts its sibling.
- Added deterministic Effect TestClock regressions for both long-running directions plus failure propagation/sibling interruption. No git or GitHub command was run; the root orchestrator retains exclusive publication, thread resolution, re-review, and merge control.
- 2026-07-18 PR #1 fourth review-remediation pass — Replaced first-match-line-only evidence slicing with one deterministic PostgreSQL-aware evidence representation per ranked source document. PostgreSQL now returns highlighted lexical match positions; nearby matches retain the existing contiguous line passage, while distant matches assemble ordered source ranges and long-line matches use exact 1-based character ranges. Tenant/project/source-version predicates, rank ordering, document limits, and exact citation validation remain unchanged.
- Added focused unit and real-PostgreSQL regressions proving terms more than six lines apart both remain in the same bounded evidence row and a match beyond character 1200 remains present with an exact source locator. No git or GitHub command was run; root orchestration retains all publication and thread-resolution work.
- 2026-07-18 PR #1 fifth review-remediation pass — Moved durable `retrieval-completed` persistence to a required typed callback at the Fred `searchText` node boundary. The search node awaits the callback immediately after deterministic retrieval succeeds and before evidence sufficiency, synthesis, or citation validation; retrieval failures cannot invoke it.
- `processOneResearchJob` now supplies the callback and owns the sanitized event payload, while `apps/worker` bridges the Effect callback into `runFredWalkingSkeleton`. `ResearchExecutionRepo.appendEvent` remains the sole persistence owner, and terminal completion/failure transactions remain unchanged.
- No git or GitHub command was run; root orchestration retains exclusive publication, review-thread resolution, re-review, and merge control.
- 2026-07-18: Completed PR #1 sixth review-remediation pass for threads `PRRT_kwDOTcucmc6SBxQe`, `PRRT_kwDOTcucmc6SBxQh`, and `PRRT_kwDOTcucmc6SBxQk`; root orchestration owns git, GitHub replies/resolution, and re-review.
- 2026-07-18 PR #1 seventh targeted hardening pass — Removed the residual non-cancellable Fred execution fallback by making `FredClientFactory.execute` mandatory. The full create/setup/execute lifecycle is now under the configured elapsed-time budget, abort liveness is checked after every non-abortable setup await, late-created clients are released, and shutdown is bounded.
- Controlled delayed create, provider setup, non-abortable retrieval, and abortable model regressions prove timeout prevents downstream registration, retrieval callbacks, model/workflow continuation, validation, and use of Fred's Promise `workflows.run` path after terminal interruption.
- Reindex state-machine audit found a stale-lease race introduced by the sixth pass: a recovered worker could complete or fail a newer claim. Worker completion and failure transitions now compare the incremented claim-attempt token; a real PostgreSQL race test proves stale attempt 1 cannot mutate attempt 2 while the current owner can complete.
- No git or GitHub command was run; root orchestration retains exclusive publication, review-thread resolution, re-review, and merge control.
- 2026-07-18 PR #1 eighth review-remediation pass — Hardened `TextRetrieval.indexText` for the trigger-created ingestion/reindex race. Exact immutable-content conflict checks now precede distinct ingestion and attempt-owned state transitions; active worker leases remain owned and stale attempts remain fenced.
- Added deterministic mock-SQL coverage and a real-PostgreSQL transaction barrier that forces the worker claim between ingestion's index upsert and state transition. No git or GitHub command was run; root orchestration retains exclusive publication, review-thread resolution, re-review, and merge control.
- 2026-07-19 PR #1 exact-head Codex remediation — Decoupled retrieval readiness from the transient `source_text_reindex_jobs` lease lifecycle. A tenant-owned immutable `source_text_index` row is now sufficient for search, so a trigger-created job claimed concurrently by the reindex worker cannot make already validated content unavailable or consume a single-attempt research job.
- Added focused regression coverage that simulates the active-lease race and fails if readiness reintroduces a reindex-status join. Missing immutable index rows still fail closed before FTS execution. No git or GitHub command was run; root orchestration retains exclusive publication, review-thread resolution, re-review, and merge control.
- 2026-07-19 final pre-merge hardening — Consolidated maintained host
  applications, workspace packages, scripts, and tests on Bun 1.3.13: native
  `bun:test`, serial PostgreSQL execution, Bun-owned
  dev/migration/build/typecheck/lint commands, and removal of direct Vitest,
  `tsx`, `concurrently`, and root Node-engine declarations. The only accepted
  DuckDB/native-dependency runtime exception is the planned pinned Phase-04
  container/service image; a host child process or host-loaded native adapter
  is prohibited.
- Exact-head Codex finding `PRRT_kwDOTcucmc6SCUnz` was remediated by making tenant-scoped immutable `source_text_index` presence sufficient for retrieval readiness during an active reindex lease. A fresh adversarial review found one documentation/future-plan inconsistency; architecture and the STEP-01-06 Execution Brief were corrected to preserve the Bun host boundary and Bun-native future commands. Root orchestration retains git, GitHub thread resolution, exact-head bot re-review, and merge.
- 2026-07-19 PR #1 root-test scope remediation (`PRRT_kwDOTcucmc6SCceo`) — Canonical `bun run test` now executes `bun test --max-concurrency 1 ./apps ./packages`, restricting discovery to maintained root workspaces while preserving every package-scoped test script. The repository contract records this scoped canonical command and no longer treats unscoped `bun test` as equivalent.
- Clean-copy proof excluded every `node_modules`, performed `bun install --frozen-lockfile` from the root only (550 packages; no `spikes/*/node_modules` created), then passed the PostgreSQL-backed canonical suite 173/173 across 27 maintained files and the no-DB suite with 145 passed, 38 intentional database skips, and 0 failures. Neither output contained a spike test path. No git or GitHub command was run; root orchestration retains publication and thread-resolution ownership.
- 2026-07-19 PR #1 persistence-ownership remediation (`PRRT_kwDOTcucmc6SCdzy`, `PRRT_kwDOTcucmc6SCdz1`) — Both ingestion in-progress ownership locks now atomically renew `job_queue.updated_at` through exact status/attempt-fenced `UPDATE ... RETURNING` operations before event or source-version persistence. A stale attempt neither renews the timestamp nor inserts its dependent row. Research in-progress event ownership now also requires `event.workspaceId` to equal the owning research job workspace, returning typed `ResearchJobOwnershipLostError` before insert on mismatch.
- Added mock-SQL ownership regressions (5/5) and isolated real-PostgreSQL regressions (3/3) for current-attempt lease renewal, stale-attempt non-renewal/non-insertion on both ingestion paths, and cross-workspace research event rejection with a successful same-workspace control. Related worker/repository unit tests passed 32/32 and the serial PostgreSQL ownership, ingestion, research, and stale-event suites passed 23/23. Lint and import boundaries passed. Full typecheck was attempted but remained blocked only by concurrent edits in excluded `upgrade.integration.test.ts` and `retrieval/src/search-text.ts`; no git or GitHub command was run.
- 2026-07-19 positional middle-support remediation — Bounded head/tail highlighting remains a memory guard, but is no longer the sole discovery mechanism for positional evidence. When sampled candidates fail, retrieval performs deterministic binary range discovery against the tenant/project/source-version-scoped immutable PostgreSQL row. Each iteration probes at most two overlapping source-contiguous ranges using numeric offsets, caps discovery at 80 probes, maps PostgreSQL character coordinates safely across UTF-16 surrogate pairs, and returns only an exact contiguous locator/excerpt of at most 1200 characters. The selected excerpt is revalidated against the original `websearch_to_tsquery` before emission.
- Added both confirmed adversarial regressions: 15 isolated `alpha` occurrences, one middle `alpha omega`, and 15 isolated `omega` occurrences separated by more than 1200 characters on one line; and 50 locator-eligible lines whose sole supported cross-line phrase is middle lines 25-26 while the first/last 12 samples omit it. Native retrieval/Fred/research focused tests passed 32/32; retrieval package typecheck and lint/import boundaries passed; real PostgreSQL retrieval/research suites passed 35/35, including exact locator and citation grounding for both new shapes. Full root typecheck remained deferred only for an unrelated concurrent ingestion-test narrowing edit owned by another unit. No git or GitHub command was run.
### 2026-07-18 — Research completion citation consistency remediation

- Hardened `ResearchExecutionRepo.complete` to validate unique citation IDs, validated citation status, run/answer alignment, and membership in the exact source-version set carried by the claimed job.
- Added an exact `FOR UPDATE` ownership read joining the persisted research run/thread, verified the persisted job payload and project against the claimed aggregate, and re-authorized the full registered source set against the job workspace/project before any terminal write.
- Removed unchecked `ON CONFLICT DO NOTHING` behavior from research result and citation completion inserts. Each insert now requires exactly one returned row; uniqueness conflicts fail with a typed `QueryError` and roll back the job, run, result, citations, and event transaction.
- Added unit regressions for duplicate citation IDs, citations outside the registered source set, source re-authorization failure, and valid exact terminal fencing.
- Added real PostgreSQL regressions proving rejection of a foreign-workspace source registration, duplicate citation IDs, preexisting result conflict, and citation-ID conflict owned by another run, including non-terminal/no-partial-write rollback assertions and a valid current completion control.
- Validation: persistence tests 43 passed / 26 integration-skipped without `DATABASE_URL`; focused real PostgreSQL ownership suite 9/9 passed; repository typecheck passed; dependency-cruiser and boundary checks passed; ESLint reported zero errors.

## Findings

- Record important facts learned during the session.
- 2026-07-18 PR #1 review remediation verified all 22 unresolved inline findings. Valid findings were corrected across tenant ownership, transactional research finalization, PostgreSQL match anchoring, Fred input typing/interruption, API error categories, startup readiness, legacy ingestion compatibility, test isolation, runtime dependencies, documentation, and Vault handoff state.
- Fred 2.0.0's Promise `workflows.run` API exposes no AbortSignal. Production execution now uses Fred's exported Effect-native workflow executor under `Effect.timeoutFail`, so expiry interrupts the workflow fiber rather than merely abandoning a Promise; the injected Promise-client path remains test-only compatibility.
- Qodo reported reviews paused and CodeRabbit's docstring-coverage warning was not one of the 22 inline actionable threads; neither represents a repository defect or implementable STEP-01-04 code finding.
- PR #1 fourth-pass finding resolved: a six-line slice anchored at only the first matching line could omit another required PostgreSQL FTS term when matches were distant, and truncating the first 1200 characters of a long matched line could omit the actual hit while overstating the line locator.
- The compatible fix is one evidence row per ranked document, preserving the request limit and deterministic `(rank DESC, sourceVersionId ASC)` ordering. A contiguous passage remains `lines:start-end`; noncontiguous passages use ordered semicolon-delimited line/character ranges whose sequence matches the excerpt segments separated by an omission marker.
- PostgreSQL `ts_headline` supplies stem-aware match offsets. The application verifies marker-stripped text exactly equals the immutable source line, bounds the assembled excerpt to 1200 characters, and fails with `RetrievalQueryError` rather than emitting an inaccurate locator when match positions cannot be represented.
- Existing citation grounding remains exact: no schema expansion was required, and citations continue to validate against the retrieved source-version/locator pair.
- PR #1 fifth-pass finding resolved: appending `retrieval-completed` only after the full Fred workflow returned delayed the milestone behind synthesis and erased it when synthesis or validation failed.
- The compatible boundary is a required `onRetrievalCompleted(evidence)` callback in `WalkingSkeletonGraphDependencies`. It is awaited after `searchText` resolves but before `requireEvidence`, so a successful zero-result retrieval records `evidenceCount: 0`, while a thrown retrieval records no completion milestone.
- Event ordering and sanitization remain worker-owned (`evidenceCount` and immutable source-version IDs only). The append completes before downstream work advances, the existing append-only journal assigns ordering, and research completion/failure transaction ownership is unchanged.
- PR #1 sixth-pass remediation resolved three findings: upgrade-safe source text indexing, end-to-end workflow cancellation, and nonblank answer decoding.
- Migration `0003_research_text_index` now creates tenant-scoped `source_text_reindex_jobs`, backfills every existing immutable `SourceVersion`, and installs an insert trigger for future versions. The worker claims restart-safe leases, reconstructs normalized text through the stored manifest, verifies UTF-8 and `content_hash`, and records bounded retry/terminal error codes when artifacts are unavailable or inconsistent.
- `TextRetrieval.indexText` writes immutable tenant-scoped index content and updates durable reindex state in one SQL transaction when it owns that transition. Search readiness follows exact tenant-scoped `source_text_index` presence; an active background reindex lease does not hide already-indexed immutable content.
- Production Fred execution now runs the Effect-native workflow through `Runtime.runPromise(fred.runtime, { signal })`. The timeout's AbortSignal is propagated through graph retrieval, durable retrieval callbacks, evidence gating, and citation validation; graph nodes assert liveness before advancing to downstream/model work.
- `retrieval-completed` uses a job-row `FOR UPDATE` ownership guard. If a late callback races terminal failure, either the event commits before failure or it is rejected after failure; it cannot appear after the terminal event.
- `ResearchAnswer.answer` now requires at least one non-whitespace character at schema decode, before citation validation or completion persistence.
- Final readiness hardening: deterministic retrieval requires every requested source version to have a tenant-scoped immutable `source_text_index` row. Indexed content remains searchable while a background reindex row is pending or in-progress; a missing index row fails closed instead of being misreported as ordinary zero evidence.
- PR #1 eighth remediation resolved `PRRT_kwDOTcucmc6SB6p0`: the ingestion fast path and independent source-text reindex worker can race after the SourceVersion insert trigger creates pending work.
- `TextRetrieval.indexText` now accepts an existing index row only when its immutable text exactly equals the proposed normalized content. Wrong tenant/project scope and conflicting content return no row and fail closed before reindex state changes.
- Reindex workers still complete only `status = 'in-progress'` with their exact incremented attempt token. The ingestion fast path can finalize pending or failed work, can observe an active in-progress lease without stealing it, and can accept an already-completed matching row idempotently.
- A controlled PostgreSQL barrier regression pauses ingestion after the immutable index upsert, claims the reindex job in that exact window, then proves ingestion succeeds while ownership stays in-progress, the current worker can complete, a completed-state ingestion retry succeeds, and conflicting content leaves both durable rows unchanged. The existing stale-attempt race continues to prove old claims cannot complete or fail newer work.
- PR #1 ninth review remediation fixed a confirmed phrase/proximity evidence defect: matched lexemes are now deduplicated only by exact source coordinate, so earlier isolated occurrences cannot erase a later PostgreSQL-supported phrase match. Unit and real-PostgreSQL regressions preserve the later `alpha omega` phrase with exact bounded locators.
- The adjacent research lease audit confirmed `citations-validated` was the sole unfenced nonterminal research event. It now uses the same atomic in-progress job-row ownership lock as `retrieval-completed`; registration and terminal events remain transactionally atomic.
- Persistence now distinguishes expected stale-worker lease loss with a sanitized `ResearchJobOwnershipLostError`. The worker treats only this typed concurrency result as a completed poll attempt; genuine terminal persistence failures still propagate and stop the combined poll loop.
- Final adversarial tightening prefers source lines that satisfy the original PostgreSQL query and uses locator-lexeme fallback only when the supported match spans lines. The real-database repeated-lexeme regression now returns only the later valid phrase (`lines:10-10`) while existing cross-line phrase and distant required-term regressions remain green.
- Independent pre-PR remediation confirmed and fixed ingestion attempt fencing: immutable SourceVersion creation, `file-processed`, `ingestion-completed`, `ingestion-failed`, and all completion/retry/failure transitions now verify the exact `(job id, entity/source id, status = in-progress, attempts)` lease. Progress events lock the owned row; terminal transitions and their event append are one transaction. Typed `IngestionJobOwnershipLostError` is the only stale-worker no-op; infrastructure failures still terminate polling.
- Reindex completion/failure now reports typed `SourceTextReindexOwnershipLostError` for expected attempt loss. The worker handles only that tag as a completed stale attempt and propagates Query/storage/retrieval infrastructure failures.
- Repetitive PostgreSQL FTS no longer accumulates every highlighted occurrence. Retrieval builds deterministic candidates capped at 1200 characters and asks PostgreSQL to revalidate them against the original `websearch_to_tsquery`; the earliest supported candidate preserves phrase, proximity, cross-line, and exact locator semantics.
- Fred preflight and execution share end-to-end elapsed limits across client creation, provider setup, registration, execution, and cleanup. A dedicated creation race handles uninterruptible Effect acquisition and signal-ignoring factories; cleanup uses remaining deadline or a fixed 10 ms emergency cap, and late-created clients are still released.
- Adjacent audit found and fixed a further confirmed lease gap: stale ingestion could otherwise insert an immutable SourceVersion after recovery. `SourceVersionRepo.createForIngestionAttempt` now locks and verifies exact attempt ownership in the same transaction as insertion.
- 2026-07-19 second independent-review remediation: phrase/proximity evidence now permits only byte-for-byte contiguous source windows. PostgreSQL query-shape detection prevents omission-separated multi-range candidates from proving positional support, while non-positional distributed terms retain accurate multi-range locators.
- Contiguous candidates preserve original source order, distance, and repeated occurrence multiplicity. Unit and real-PostgreSQL regressions prove earlier isolated `alpha`/`omega` locations cannot masquerade as a later cross-line phrase, and `"alpha alpha"` spanning two adjacent lines longer than 1200 characters returns exact bounded line/character locators.
- Exhausted stale ingestion recovery now changes job status and inserts one sanitized `ingestion-failed` event inside the same repository transaction. The event ID is deterministic per job attempt; an injected event-write failure rolls back both exhausted failure and requeue changes, successful recovery is idempotent, and infrastructure failures propagate.
- Canonical test-runtime direction changed during this pass: Bun is the sole validation runtime going forward. Existing tooling/scripts were not removed as part of this bounded remediation.

## Context Handoff

- Use this as the single canonical prose section for prepared context, resume notes, and handoff summaries tied to the current effective context.
- Keep durable conclusions promoted into phase, bug, decision, or architecture notes when they outlive the session.
- Delivery workflow addition: once Phase 01 completes and merges, Phase 02 must be refined and vault-validated before its first step branch is created. The same gate applies at every later phase boundary. Phase 01 is already active and its step notes are refined, so STEP-01-04 continues under the existing phase context after design approval.
- Orchestration mode is now authoritative: STEP-01-04 and every later unit runs in a fresh subagent with no git access; the root orchestrator owns validation, retries (3 total fresh attempts), branch/PR/review/merge operations, and phase advancement. No interactive approval pauses. Stop immediately before v1.0 release.
- STEP-01-04 implementation is complete with no known confirmed defects. Repository, real-database, provider-load, documentation, security-scan, and Vault gates passed.
- Root orchestrator owns all Git inspection, branch publication, review remediation, and merge work. This worker ran no Git command.
- The existing live SSE placeholder is an explicit later-phase follow-up; this step durably records observable research progress in `event_journal`.

## Changed Paths

<!-- AGENT-START:session-changed-paths -->
- Domain schemas/errors; persistence migration/repositories; `packages/retrieval`, `packages/research-engine`, and `packages/fred-workflows`; API research route; worker research job/runtime wiring; ingestion indexing; unit/integration tests; workspace manifests/lockfile; README, environment example, and architecture/local-development/repository-contract documentation.
- Step implementation/outcome notes and architecture summaries were updated in Agent Vault.
<!-- AGENT-END:session-changed-paths -->
- PR #1 second remediation: `apps/worker/src/entrypoint.test.ts`; `packages/fred-workflows/src/adapters/fred-runtime.ts`; `packages/fred-workflows/test/walking-skeleton.test.ts`; `packages/retrieval/src/search-text.ts`; `packages/retrieval/test/search-text.test.ts`; `apps/api/src/routes/research.integration.test.ts`; `README.md`.
- PR #1 third remediation: `apps/worker/src/main.ts`; new focused scheduling boundary `apps/worker/src/polling.ts`; deterministic regression coverage `apps/worker/src/polling.test.ts`; this session note; generated code graph/index context from Vault refresh.
- `packages/retrieval/src/search-text.ts` — PostgreSQL-highlighted match collection, bounded contiguous/composite evidence assembly, exact line/character locators, and typed fail-closed evidence errors.
- `packages/retrieval/test/search-text.test.ts` — focused distant-term and late-long-line regressions plus PostgreSQL headline contract coverage.
- `apps/api/src/routes/research.integration.test.ts` — real-PostgreSQL fixtures and citation-grounding regressions for distant terms and late long-line matches.
- `.agent-vault/05_Sessions/2026-07-18-201147-implement-deterministic-retrieval-and-fred-research-workflow-step-01-04-implementor.md` — fourth PR remediation handoff, findings, validation, and changed-path evidence.
- `packages/fred-workflows/src/graphs/walking-skeleton.ts` — required typed retrieval-completion callback awaited at the successful search boundary.
- `packages/fred-workflows/test/walking-skeleton.test.ts` — successful empty retrieval and failed retrieval callback-boundary regressions.
- `apps/worker/src/jobs/run-research.ts` — worker-owned durable milestone callback and removal of the delayed post-workflow append.
- `apps/worker/src/jobs/run-research.test.ts` — blocked-synthesis visibility and retained-on-failure regression.
- `apps/worker/src/main.ts` — Effect-to-Promise callback bridge through `runFredWalkingSkeleton`.
- `apps/api/src/routes/research.integration.test.ts` — real-PostgreSQL in-progress visibility, final ordering, and zero-result milestone coverage.
- `.agent-vault/05_Sessions/2026-07-18-201147-implement-deterministic-retrieval-and-fred-research-workflow-step-01-04-implementor.md` and generated Vault context/code graph — fifth remediation handoff and refreshed symbol map.
- Sixth-pass remediation: `packages/persistence/src/migrations/0003_research_text_index.sql`, `.down.sql`, migration upgrade/runner tests, `packages/persistence/src/repositories/source-text-reindex.ts` and tests/exports, `packages/retrieval/src/search-text.ts` and tests, `apps/worker/src/jobs/reindex-source-text.ts` and tests, worker polling/main, research worker/repository/tests, Fred runtime/graph/tests, domain schema/tests, API real-DB research regression, and `docs/local-development.md` / `docs/architecture.md`.
- Seventh-pass hardening: `packages/fred-workflows/src/adapters/fred-runtime.ts` and `packages/fred-workflows/test/walking-skeleton.test.ts`; `packages/retrieval/src/search-text.ts` and `packages/retrieval/test/search-text.test.ts`; `packages/persistence/src/repositories/source-text-reindex.ts` and its unit test; `apps/worker/src/jobs/reindex-source-text.ts` and its unit test; `apps/api/src/routes/research.integration.test.ts`; this session note and refreshed generated Vault context.
- Second independent-review remediation: `packages/retrieval/src/search-text.ts`, `packages/retrieval/test/search-text.test.ts`, `packages/persistence/src/repositories/interfaces.ts`, new `packages/persistence/src/repositories/job-event.integration.test.ts`, `packages/persistence/src/repositories/job-event.test.ts`, `apps/worker/src/jobs/ingest-source.ts`, `apps/worker/src/jobs/ingest-source.test.ts`, `apps/worker/src/main.ts`, `apps/api/src/routes/sources.integration.test.ts`, `apps/api/src/routes/research.integration.test.ts`, `docs/architecture.md`, and this bounded Vault handoff.
- Final exact-head/runtime hardening: `packages/retrieval/src/search-text.ts` and retrieval regression tests; root/workspace manifests, `bun.lock`, `bunfig.toml`, ESLint config, all maintained test imports, Bun-owned scripts, removal of Vitest configs; runtime-command documentation; `docs/architecture.md`; and STEP-01-06 future execution instructions/runtime boundary.

## Validation Run

<!-- AGENT-START:session-validation-run -->
- Command set: `bun install --frozen-lockfile`; root typecheck/lint/import-boundary/build/test/secrets/docs gates; `docker compose config --quiet`; PostgreSQL migration up/down/up; provider package import smoke from `packages/fred-workflows`; real database integration suite.
- Result: PASS. Full unit suite: 138 passed, 17 database-only skipped, 0 failed (605 assertions). Real PostgreSQL integration: 11 passed, 0 failed.
- Notes: grounded completion, exact citations, insufficient evidence, stale recovery, scope isolation, durable job/run terminal state, and ordered research event families are covered.
<!-- AGENT-END:session-validation-run -->
- Retry-2 focused boundary gates: `npx vitest run packages/fred-workflows/test/walking-skeleton.test.ts --reporter=verbose` (2 passed), `bun test packages/fred-workflows/test/walking-skeleton.test.ts` (2 passed), and `bun run --filter @struct/fred-workflows typecheck` passed.
- Retry-2 complete root gates: `bun install --frozen-lockfile`; `bun run typecheck`; `bun run lint`; `bun run lint:imports`; `bun run build`; `npx vitest run` (97 passed, 11 database-only skipped); `bun test` (138 passed, 17 database-only skipped); `bun run secrets:scan`; `bun run docs:lint`; `docker compose config --quiet`; and provider-package Bun import smoke all passed.
- Test-isolation proof: complete Node Vitest and raw Bun suites passed when launched concurrently after replacing fixed smoke ports with ephemeral ports.
- PostgreSQL gates: migration `up/down/up` passed; the focused repository/source-ingestion/research integration suite passed 11/11 under both Node Vitest and raw Bun.
- Vault generated context/code graph was refreshed and strict validation completed with zero errors and zero warnings.
- Result: PASS. STEP-01-04 is completed with mirrored `context_status: completed` and zero known confirmed defects.
- Root-orchestrator verification: frozen install, typecheck, lint, import boundaries, Node Vitest (97 pass / 11 DB-only skipped), raw Bun (138 pass / 17 DB-only skipped), build, canonical secrets/docs scripts, Compose config, real-DB Node suite (108/108), real-DB raw Bun suite (149/149), migration down/up, Fred provider load, and Vault doctor all passed.
- Retry-3 hygiene gates passed: root `typecheck`, ESLint, dependency/import boundaries, build, Node Vitest (97 passed, 11 database-only skipped), raw Bun (138 passed, 17 database-only skipped), canonical secrets/docs scripts, `docker compose config --quiet`, and Fred/Fred OpenAI package-load smoke from `packages/fred-workflows`.
- Final post-validation filesystem audit confirmed the three STEP-01-04 `tsconfig.tsbuildinfo` files are absent, `*.tsbuildinfo` is ignored repository-wide, and no related cache artifact remains. STEP-01-04 and its mirrored context remain completed with zero known confirmed defects.
- PR review remediation focused gates: root TypeScript compilation passed; 26 focused Node Vitest tests covering retrieval, research-engine, Fred workflow, API registration, ingestion, and research jobs passed after interface corrections. Full repository, real-PostgreSQL, migration, build, and strict Vault gates follow in this handoff.
- Final PR-remediation gates PASS: frozen Bun install; root typecheck; zero-warning ESLint; dependency/import boundaries; build; Node Vitest 103 passed / 12 database-only skipped; raw Bun 144 passed / 18 database-only skipped; readiness-driven worker smoke under Node and Bun; canonical secrets/docs scripts; Compose validation; migration down/up; Fred provider preflight; real-PostgreSQL Node 12/12 and raw Bun 12/12; refreshed code graph (54 files, 572 symbols); refreshed authoritative context/indexes; and Vault doctor/all checks with 0 errors and 0 warnings.
- Regression coverage proves tenant ownership is derived from immutable source relationships, excerpts use PostgreSQL match-line metadata including stem-only matches, legacy ingestion payloads remain processable, Fred input and empty-evidence failures remain typed, production Fred timeout uses Effect-native fiber interruption, startup validates provider config before readiness, polling is concurrent, API client/infrastructure failures stay distinct, and stale recovery cannot race terminal completion or leave split state.
- PR #1 second remediation gates PASS: root typecheck; zero-warning ESLint; dependency/import boundaries; focused Node tests (10/10); real-PostgreSQL cross-line term and phrase regressions under Node and Bun (5/5 each); full Node Vitest with PostgreSQL (117/117); full raw Bun with PostgreSQL (158/158); production build; Compose validation; canonical secrets/docs scripts; migration down/up; and post-migration real-database regression (5/5).
- Regression coverage proves an injected Fred factory hook that ignores `maxElapsedMs` is still timed out by the product runtime and releases the Fred client; PostgreSQL `websearch_to_tsquery` document matches survive when required terms or a quoted phrase cross newline boundaries; returned excerpts begin at the first relevant lexeme line.
- PR #1 third remediation gates PASS: focused worker typecheck; focused Node Vitest 3/3 and raw Bun 3/3; root typecheck; zero-warning repository ESLint; dependency/import boundaries; full Node Vitest without PostgreSQL 106 passed / 14 skipped; full raw Bun without PostgreSQL 147 passed / 20 skipped; production build; canonical secrets/docs scripts; Compose validation; migration down/up; full real-PostgreSQL Node 120/120 and raw Bun 161/161; Fred provider package-load smoke.
- Regression coverage proves a blocked research poll does not delay the next ingestion interval, a blocked ingestion poll does not delay the next research interval, and either poll's failure remains fatal to the combined worker loop while interrupting its sibling.
- PR #1 fourth remediation gates PASS: focused retrieval/research Node Vitest 8/8; focused real-PostgreSQL Node and raw Bun 15/15; root TypeScript compilation; zero-warning ESLint; dependency/import boundaries; full Node Vitest with PostgreSQL 124/124; full raw Bun with PostgreSQL 165/165; production build; frozen install with no changes; Compose validation; canonical secrets/docs scripts; migration down/up; post-migration real-PostgreSQL Node and Bun 7/7 each; Fred/Fred OpenAI provider package-load smoke.
- Regression evidence proves a document match with required terms on lines 2 and 10 returns one bounded row containing both terms with locator `lines:2-2;lines:10-10`; a long-line match after character 1200 is centered in the excerpt, maps exactly through `line:1,chars:start-end`, stays within 1200 characters, and passes exact citation validation without grounding mismatch.
- PR #1 fifth remediation gates PASS: focused Fred/worker Node Vitest 9/9; focused real-PostgreSQL Node and raw Bun 8/8 each before and after migration round-trip; root TypeScript compilation; zero-warning ESLint; dependency/import boundaries; full Node Vitest with PostgreSQL 127/127; full raw Bun with PostgreSQL 168/168; production build; frozen install with no changes; Compose validation; canonical secrets/docs scripts; migration down/up; Fred/Fred Effect/Fred OpenAI provider imports.
- Regression coverage proves `retrieval-completed` is durably visible while synthesis is blocked, remains ordered before `research-failed` when synthesis later fails, records a successful zero-result retrieval before evidence insufficiency, and is absent when deterministic retrieval itself throws.
- PR #1 sixth remediation gates PASS: focused Node 41/41 and raw Bun 41/41; real PostgreSQL migration down/up; isolated legacy-schema upgrade fixture; focused real-DB Node and Bun 18/18; full PostgreSQL Node Vitest 138/138; full PostgreSQL raw Bun 179/179; frozen install; root TypeScript; zero-warning ESLint; dependency/import boundaries; production build; Compose; canonical secrets/docs scripts; and Fred/Fred Effect/Fred OpenAI provider imports.
- Regression evidence proves existing SourceVersions are durably queued at upgrade with tenant/artifact/hash state; new SourceVersions are trigger-queued; unavailable artifacts remain explicitly retryable/failed; successful indexing atomically completes reindex state; timeout interrupts retrieval and prevents retrieval-completed/model work; a terminal job rejects late retrieval callbacks in unit and real PostgreSQL coverage; and empty/whitespace ResearchAnswer values fail schema decode.
- Historical pre-readiness-correction checkpoint: PostgreSQL Node 139/139 and unscoped raw Bun 180/180 passed with the then-current rule that pending reindex state failed before FTS. That readiness rule was later superseded: tenant-scoped immutable index presence is authoritative, including during an active background lease.
- PR #1 seventh hardening gates PASS: focused cancellation/reindex Node Vitest 22/22; root TypeScript compilation; zero-warning ESLint; dependency/import boundaries; production build; frozen install with no changes; full Node Vitest without PostgreSQL 126 passed / 19 skipped; full raw Bun without PostgreSQL 167 passed / 27 skipped; Compose validation; canonical secrets/docs scripts; migration down/up; full PostgreSQL Node Vitest 145/145; full PostgreSQL raw Bun 186/186; and Fred/Fred Effect/Fred OpenAI provider-load smoke.
- Regression evidence proves a client created after timeout is released without provider setup; delayed provider resolution cannot continue tool/agent/workflow registration; delayed retrieval resolution cannot emit `retrieval-completed` or reach model work; delayed model execution is interrupted without validation or workflow completion; and no `workflows.run` fallback remains. The PostgreSQL lease-race regression proves stale reindex completion and failure both fail while the current attempt remains in-progress and can complete.
- PR #1 eighth remediation gates PASS: root TypeScript compilation; zero-warning ESLint; dependency/import boundaries; production build; Compose validation; canonical secrets/docs scripts; focused retrieval Node and Bun 8/8 each; controlled real-PostgreSQL research/reindex Node and Bun 10/10 each; full PostgreSQL Node Vitest 147/147; and full PostgreSQL raw Bun 188/188.
- Regression evidence proves exact-content idempotency for both active and completed reindex state, preservation of the current attempt lease during an ingestion race, rejection of conflicting immutable text without durable mutation, and continued stale-attempt fencing. Generated package `tsconfig.tsbuildinfo` artifacts were removed after validation.
- PR #1 ninth remediation gates PASS: focused Node Vitest 20/20 and raw Bun 20/20; focused real-PostgreSQL Node and Bun 12/12 each; root/package TypeScript compilation; zero-warning ESLint; dependency/import boundaries; production build; frozen install with no changes; Compose validation; canonical secrets/docs scripts; migration down/up plus post-migration Node 13/13; full PostgreSQL Node Vitest 153/153; and full PostgreSQL raw Bun 194/194.
- Regression evidence proves earlier isolated `alpha`/`omega` occurrences cannot remove a later quoted-phrase match, citation locators remain exact and bounded, stale recovery fences both nonterminal research events and terminal completion, typed ownership loss does not terminate subsequent polling, and non-ownership terminal persistence failure remains fatal. Ignored TypeScript build-info artifacts were removed after validation.
- Independent pre-PR remediation gates: focused Node Vitest 49/49 and raw Bun 49/49; controlled real-PostgreSQL ingestion/research 17/17 and focused Bun DB 66/66; full PostgreSQL Node Vitest 167/167 across 26 files; full PostgreSQL raw Bun 208/208 across 32 files. Regression coverage includes exact stale-attempt rejection for SourceVersion create/event/complete/pending/fail, current-attempt success, typed reindex ownership no-op versus fatal infrastructure error, 400-occurrence term and 300-occurrence phrase evidence, exact citations, never-resolving Fred create/provider, late clients, and deadline-capped hanging cleanup.
- Static/runtime gates PASS: frozen install with no changes; root typecheck; zero-warning ESLint; dependency/import boundaries; production build; Compose config; canonical secrets/docs scripts; Fred/Fred Effect/Fred OpenAI provider imports. Migration down/up and post-migration upgrade plus ingestion/research verification passed 18/18.
- Historical orchestration checkpoint: STEP-01-04 and its mirror remained `in-progress` at this point pending the then-required second independent review; both were completed after that gate passed.
- Historical pre-scope raw Bun checkpoint: focused behavior 47/47 and unscoped PostgreSQL discovery 213/213 passed before root test discovery was restricted to maintained `apps` and `packages`. This checkpoint is retained as history, not as the canonical maintained-host gate.
- Post-migration PostgreSQL suites were rerun serially under raw Bun to avoid shared-fixture collisions: legacy upgrade 1/1, core repositories 7/7, stale ingestion fault boundary 1/1, ingestion route 3/3, and research/retrieval 16/16.
- The stale recovery fault test injects an `event_journal` write failure and proves no split terminal status can commit; retry persists one sanitized event and repeat recovery creates no duplicate. Retrieval regressions inspect exact locator reconstruction and the 1200-character bound.
- Historical orchestration checkpoint: STEP-01-04 and its mirror still remained `in-progress` here pending the final independent review; both were subsequently completed. Bun remained the canonical test runtime for later gates.
- PR #1 exact-head readiness remediation gates PASS under canonical Bun: retrieval behavior 14/14 (57 assertions), source-text reindex repository 3/3 (10 assertions), isolated PostgreSQL legacy-upgrade/reindex fixture 1/1 (3 assertions), retrieval package TypeScript compilation, and production retrieval-source ESLint. The regression proves a populated immutable index remains searchable during an active background lease, while absence of the index remains a typed retrieval failure.
- Historical final unscoped raw Bun checkpoint: PostgreSQL discovery passed 214/214 with 917 assertions across 33 files before canonical root scoping. The subsequent clean-copy, maintained-host root gate is authoritative: `bun run test` passed 173/173 across 27 maintained files with no spike paths discovered.
- Dependency audit confirms Vitest and `concurrently` are absent; `tsx` remains only as Vite 8's optional transitive peer and is not directly declared or invoked. Focused readiness coverage proves indexed immutable content remains searchable while its background reindex lease is active, and missing index rows still fail closed.
- Final root-owned canonical gate (2026-07-19): `bun install --frozen-lockfile`, typecheck, lint, import/boundary checks, all application builds, Compose validation, migration 0004 down/up, existing secrets/docs commands, and `DATABASE_URL=postgresql://struct:struct@localhost:5432/struct bun run test` all passed. Test result: 262/262, 1,506 assertions, 40 maintained files. `git diff --check` passed.

## Bugs Encountered

<!-- AGENT-START:session-bugs-encountered -->
- None.
<!-- AGENT-END:session-bugs-encountered -->

## Decisions Made or Updated

<!-- AGENT-START:session-decisions-made-or-updated -->
- None.
<!-- AGENT-END:session-decisions-made-or-updated -->

## Follow-Up Work

<!-- AGENT-START:session-follow-up-work -->
- [x] Complete [[02_Phases/Phase_01_walking_skeleton/Steps/Step_04_implement-deterministic-retrieval-and-fred-research-workflow|STEP-01-04 Implement Deterministic Retrieval and Fred Research Workflow]] implementation and local review remediation.
<!-- AGENT-END:session-follow-up-work -->
- [ ] Root orchestrator publishes this remediation to PR #1, resolves or replies to all verified threads with evidence, waits for automated re-review, and merges only after the zero-defect gate passes.
- [ ] After merge, advance to [[02_Phases/Phase_01_walking_skeleton/Steps/Step_05_stream-persisted-progress-and-render-navigable-citation|STEP-01-05 Stream Persisted Progress and Render Navigable Citation]].

## Completion Summary

- STEP-01-04 implementation and all 22 PR #1 inline review findings were addressed locally with regression coverage. The root orchestrator retains publication, thread resolution/replies, automated re-review, and merge ownership.
- Seventh-pass cancellation and reindex lease hardening is complete with unit and real-PostgreSQL regression coverage. Zero known confirmed defects remain in the reviewed STEP-01-04 tree; root orchestration owns publication and automated re-review.
- 2026-07-19 PR #1 stale-research remediation: active Fred work now renews an exact `(job id, run id, workspace id, attempt, in-progress)` lease at a bounded heartbeat interval. Recovery measures lease age against PostgreSQL `NOW()` so replicas share one clock; ownership loss interrupts the old workflow without a late failure/completion write, while heartbeat infrastructure failures remain fatal.
- Worker startup now rejects unsafe `WORKER_JOB_STALE_MS` / `RESEARCH_MAX_ELAPSED_MS` / poll combinations with typed `ResearchJobTimingConfigurationError`. The required threshold reserves `max(30000, 2 * WORKER_POLL_INTERVAL_MS)` beyond the Fred deadline, and valid deployments heartbeat at most every 10 seconds.
- Focused evidence: native Bun worker/config tests 19 passed; real-PostgreSQL research integration 16 passed, including the just-younger-than-stale boundary, database-clock recovery, attempt-fenced renewal, and late-terminal rejection; workspace typecheck, ESLint, import boundaries, and docs lint passed.
- 2026-07-19 documentation/test-integrity remediation: session and Outcome handoffs now identify scoped maintained-host native Bun/PostgreSQL 173/173 across 27 files as authoritative, with 213/213 and 214/214 retained only as historical unscoped checkpoints. Current Code Map records native `bun:test` and ESLint 9.39.2.
- Upgrade guidance preserves reindex `attempts`, requeues only rows with remaining budget, and states current readiness truth: a tenant-scoped immutable index is searchable during pending/in-progress background work, while a missing index fails closed.
- The isolated PostgreSQL upgrade regression now asserts workspace, project, artifact reference, content hash, pending status, and zero initial attempts for both migration-backfilled and trigger-created jobs.
- PR #1 merged into `main` as merge commit `1057a98` after CodeRabbit succeeded on exact head `a9862db` and every Codex thread was addressed and resolved.
- Final repository gate: 264 tests passed, 0 failed, 1,515 assertions; typecheck, lint, import boundaries, builds, Compose config, migrations down/up, diff check, and strict Vault validation all passed.
- STEP-01-04 is complete. Continue with STEP-01-05 on its own branch and PR.
